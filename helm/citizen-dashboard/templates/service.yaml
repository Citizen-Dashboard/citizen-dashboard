apiVersion: v1
kind: Service
metadata:
  name: {{ include "citizen-dashboard.fullname" . }}
  labels:
    {{- include "citizen-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "citizen-dashboard.selectorLabels" . | nindent 4 }}
