apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "citizen-dashboard.name" . }}-search-api
  labels:
    app.kubernetes.io/name: {{ include "citizen-dashboard.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: search-api
spec:
  replicas: {{ .Values.search-api.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "citizen-dashboard.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: search-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "citizen-dashboard.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: search-api
    spec:
      containers:
        - name: search-api
          image: "{{ .Values.images.search-api.repository }}:{{ .Values.images.search-api.tag }}"
          imagePullPolicy: {{ .Values.images.search-api.pullPolicy }}
          ports:
            - containerPort: {{ .Values.search-api.containerPort }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.search-api.containerPort }}
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 1
            successThreshold: 1
            failureThreshold: 3
      imagePullSecrets:
        - name: ghcr-secret
